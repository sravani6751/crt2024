
#include <stdio.h>

int main() {
    // Write C code here
    int i,n;
    scanf("%d",&n);
    i=1;
    while(i<=n)
    {
        if(i%2==0)
        {
            printf("%d",i);
        }
        i++;
    }

    return 0;
}
