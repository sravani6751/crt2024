#include <stdio.h>

int main() {
    // Write C code here
    int i,n,sum;
    scanf("%d",&n);
    i=1;
    sum=0;
    while(i<=n)
    {
        if(i%2==0)
        {
        sum=sum+i;
        }
    printf(" i= %d sum=%d\n ",i,sum);
    i++;
    }
    return 0;
}
